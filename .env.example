# ElevenLabs API key — OPTIONAL.
# Only needed to (re)generate music/SFX via scripts/gen-music.mjs and
# scripts/gen-sfx.mjs. The example videos render silently without it.
# A restricted key (Music Generation + Sound Effects scopes) is enough.
# Never commit the real key.
ELEVENLABS_API_KEY=
